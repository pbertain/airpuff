---
# PostgreSQL Setup Role - Main Tasks

- name: Add PostgreSQL APT repository
  apt_repository:
    repo: "deb http://apt.postgresql.org/pub/repos/apt/ {{ ansible_distribution_release }}-pgdg main"
    state: present
    update_cache: yes
  tags: [postgresql, repository]

- name: Add PostgreSQL signing key
  apt_key:
    url: https://www.postgresql.org/media/keys/ACCC4CF8.asc
    state: present
  tags: [postgresql, repository]

- name: Install PostgreSQL
  apt:
    name:
      - "postgresql-{{ postgresql_version }}"
      - "postgresql-client-{{ postgresql_version }}"
      - "postgresql-contrib-{{ postgresql_version }}"
      - "postgresql-server-dev-{{ postgresql_version }}"
    state: present
  tags: [postgresql, packages]

- name: Install TimescaleDB repository
  apt_repository:
    repo: "deb https://packagecloud.io/timescale/timescaledb/ubuntu/ {{ ansible_distribution_release }} main"
    state: present
    update_cache: yes
  tags: [timescaledb, repository]

- name: Add TimescaleDB signing key
  apt_key:
    url: https://packagecloud.io/timescale/timescaledb/gpgkey
    state: present
  tags: [timescaledb, repository]

- name: Install TimescaleDB
  apt:
    name: "timescaledb-2-postgresql-{{ postgresql_version }}"
    state: present
  tags: [timescaledb, packages]

- name: Configure PostgreSQL
  include_tasks: configure.yml
  tags: [postgresql, configure]

- name: Create AirPuff database
  include_tasks: database.yml
  tags: [postgresql, database]

- name: Configure TimescaleDB
  include_tasks: timescaledb.yml
  tags: [timescaledb, configure]

- name: Start and enable PostgreSQL
  systemd:
    name: postgresql
    state: started
    enabled: yes
  tags: [postgresql, service]
